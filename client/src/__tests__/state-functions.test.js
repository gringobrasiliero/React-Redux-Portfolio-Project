import {Vote} from '../components/votes';


describe('Addition', () => {
  it('knows that 2 and 2 make 4', () => {
    expect(2 + 2).toBe(4);
  });
});


test('handleUpvote adds 1 to vote', () => {
});
